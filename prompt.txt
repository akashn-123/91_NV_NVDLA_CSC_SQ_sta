sources/netlist.v currently fails timing. Optimize the RTL so that post-synthesis WNS is  ≥ 0 ns, using the standard cell library at /lib/nangate45nm.lib. RTL restructuring must be the first approach. Direct netlist modifications via sources/eco_fix.py are only permitted once post-synthesis WNS is ≥ −0.40 ns. Netlist changes made while WNS is worse than −0.40 ns will not be accepted. Do not change functional behavior. See Specification.md for the RTL functionality and timing report structure.
