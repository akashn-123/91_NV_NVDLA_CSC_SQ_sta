sources/netlist.v currently fails timing. Optimize the RTL so that post-synthesis WNS  ≥ 0 ns. Use sources/synth.tcl for synthesis (Yosys) and OpenSTA for static timing analysis with the standard cell library at /lib/nangate45nm.lib. The grader reads the output files produced by these tools — other synthesis or STA methods will not be accepted. RTL restructuring must bring WNS to ≥ −0.40 ns before any direct netlist modifications are made. Once that threshold is reached, ECO cell swapping via sources/eco_fix.py is permitted. Netlist changes made while WNS is worse than −0.40 ns will not be accepted.
Do not change functional behavior. See Specification.md for RTL functionality and timing report structure.
